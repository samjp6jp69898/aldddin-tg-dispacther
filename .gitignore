node_modules/
.env
.env.*
# 2026-08-31：新增 repo-local .env（原本讀根目錄 .env），.env.example 是無值範本，
# 要能進 git 給其他機器/人參考該填哪些欄位。
!.env.example
*.log

# T15：token 讀取衛生檢查發現，只擋 dotenv 格式（.env*）跟 *.log 對「非
# dotenv 格式的誤存敏感檔名」沒防護——目前沒有任何程式碼路徑會產生這些檔案，
# 純粹是未來手滑的防呆，不是已知風險。刻意不用 *token*/*secret* 這種廣泛
# 萬用字元：會連帶誤傷合法原始碼檔名（例如未來真的有 xxxToken.ts）。
secrets.*
credentials.*
config.env
*.pem
*.key
logs/
# T36（demand plan pipeline）執行期產物：plan.md 上傳 Drive 後才是正式交付，
# 本機這份只是留痕方便除錯，跟 logs/ 同一類，不進版本控制。
demand-plans/
